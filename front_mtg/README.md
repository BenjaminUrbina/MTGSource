# front_mtg

Minimal React + TypeScript + Vite base for the MTGSource frontend.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## API

- Default API base URL: `/api`
- Override with `VITE_API_BASE_URL`
- Local Vite dev server proxies `/api` to `http://127.0.0.1:8000`
